# 事前登録：モデル改訂（2026-09-03）

**数字を見る前に、ここに書いたことだけを試す。あとから足さない。**

---

## なぜ事前登録するか

前回までに「回収率100%超」を8回記録して8回とも失った。
原因のひとつが **③ 探索による選択** で、たくさん試せば良い数字は必ず出る。

実測でも確かめている。払戻をシャッフルして中身をゼロにしたうえで、
4つの帯から一番良いものを選ぶと **平均142% / 最大216%** が出た。
つまり「一番良い構成を選ぶ」という行為そのものが、無から数字を作る。

だから、**何を試すかを先に決めて、その全部を報告する。**

---

## いまの状態（出発点）

壊れていた元データ（クラス100%欠損・賞金27.7%欠損・回り27.7%欠損）を直し、
特徴量・モデル・検証データを作り直した直後の値。

```
全体   10,374点  107.6%   95%区間 [ 94%, 123%]
単勝    7,121点  114.9%   95%区間 [ 98%, 136%]
ワイド   3,253点   91.7%   95%区間 [ 69%, 117%]
```

**95%区間が100%を含む。いまは「黒字だ」と言えない状態。**

---

## 検証の枠組み

### 開発と評価を分ける

```
開発に使う   2021 - 2024        ここで全部の試行を行う
最終評価     2025               **最後に1回だけ**見る
```

2025年は開発中に一切見ない。すべての候補を2021-2024で決めきってから、
選んだ1つだけを2025年で測る。

⚠ 2025年を見たあとに構成を変えたら、その時点でこの枠組みは無効になる。
  そのときは「無効になった」と明記する。

### 学習は毎年やり直す（既存の仕組みのまま）

その年を測るときは、その年より前のデータだけで学習する。
`resid_kinds.py` が既にこの形。ここは変えない。

### 順列検定

選んだ構成について、レース内でスコアをシャッフルして同じ検定を回す。
シャッフルで同等の数字が出るなら、その構成に中身はない。

---

## 試すこと（この5つだけ。あとから足さない）

### A. 直した3列は実際に効いているか

クラス・回り・賞金を直したが、モデルの成績は下がった。
**これらの列がそもそも役に立っていない可能性がある。**

| | 中身 |
|---|---|
| A-1 | いまの全特徴量（基準） |
| A-2 | クラス系を除く（クラス_num・クラス変化・距離×クラス系） |
| A-3 | 賞金系を除く（過去獲得賞金累計・過去平均獲得賞金とその派生） |
| A-4 | 回り系を除く（回り_num・回り別_過去勝率/複勝率とその派生） |

**判断**: 除いて改善するなら、その列は雑音として働いている。

### B. 券種

ワイドが 91.7% で、区間の上限が117%。単勝だけのほうが良く見える。
ただし**見えるだけでは採用しない。**

| | 中身 |
|---|---|
| B-1 | 単勝＋ワイド（基準） |
| B-2 | 単勝のみ |

**判断**: 2021-2024で単勝のみが優れ、かつ差が偶然で説明できない場合のみ採用。

### C. しきい値 AX_GAP

いまの 1.5 は**壊れたデータで決めた値**なので、根拠が失われている。

| | 中身 |
|---|---|
| C | 1.3 / 1.5 / 1.7 / 2.0 の4つを測る |

⚠ **一番良い値は選ばない。** 4つの結果を全部載せ、
「点数と回収率の関係が単調か」を見る。単調でなければ雑音と判断して 1.5 のまま。

### D. シードを増やす

いまは3シードの平均。1シードだと12.3pt振れる。

| | 中身 |
|---|---|
| D | 3シード（基準）と 7シード |

**判断**: 7シードで区間が明確に狭くなるなら採用。回収率が上がるかは見ない。

### E. 学習量

`N_ROUNDS = 600` の根拠が記録に無い。

| | 中身 |
|---|---|
| E | 300 / 600 / 1200 |

⚠ **一番良い値は選ばない。**C と同じく、単調でなければ 600 のまま。

---

## 採用の規則（先に決める）

1. 2021-2024 の回収率だけでは採用しない。**95%区間が100%を上回ること。**
2. C と E は、単調な関係が見えたときだけ動かす。山の頂点は選ばない。
3. どれも条件を満たさなければ、**何も変えない。**
   「変えない」も結論として報告する。
4. 選んだ構成を 2025 で1回だけ測る。そこで下がっても、選び直さない。

---

## 報告すること

- 試した全構成の数字（良いものだけ載せない）
- 2025年での最終評価
- 順列検定の結果
- 何も改善しなかった場合は、そう書く

---

## この検証で分からないこと

- 2021-2024で選んだ構成が、2026年以降も通用するか
- 必要点数（単勝で約9,400点＝79か月）は変わらない。
  **今回の作業で「黒字を証明できる」ようにはならない。**
  できるのは「いまの構成より悪くない構成を選ぶ」ことだけ。

---

# 結果（2026-09-03）

## 採用した変更：**なし**

事前登録の規則3「どれも条件を満たさなければ、何も変えない」を適用した。

| | 判定 | 理由 |
|---|---|---|
| A 特徴量 | 変更なし | 外すとどれも悪化（クラス-16.7pt / 回り-18.5pt / 賞金-4.3pt） |
| B 券種 | 変更なし | 単勝＋ワイドが最良 |
| C しきい値 | 変更なし | 1.5が既に最短（証明まで23か月） |
| D シード | 変更なし | 7にしても区間が狭くならない（45.9→45.8） |
| E 学習量 | **変更なし** | 開発では300が良かったが、2025年で逆転した |

## 2025年（一度きりの評価）

```
採用案 rounds=300   1,727点   82.1%   95%[63.4, 104.6]
現行  rounds=600   2,029点   87.2%   95%[65.3, 113.5]
```

**両方とも100%を割った。**開発（2021-2024）では 300=141.7% / 600=132.0% だったので、
**開発での差は偶然だった。**向きまで逆転している。

これが2025年を最後まで見なかったことの価値。見ながら選んでいたら、
「300のほうが良い」という誤った結論を採用していた。

## 分かったこと

### 1. データ修正は正しかった

クラス・回り・賞金を外すと、いずれも成績が落ちる。
7月以降クラスが100%欠けたまま動いていた損失は実在した。

### 2. モデルには本物の選別力がある

順列検定（レース内でgapをシャッフル）。

```
              シャッフル   実測      実測以上が出る確率
gap>=1.3      68.0%    116.5%      0.0%
gap>=1.5      67.7%    127.5%      0.0%
gap>=1.7      67.1%    144.1%      0.0%
gap>=2.0      64.4%    186.5%      0.0%
```

シャッフルは横ばい（68.0→64.4）。実測は単調に上がる。
**gapの順位には確かに情報がある。**修正前は 107/88.5/161/185 と単調ですらなかった。

### 3. それでも黒字は示せない

```
5年通し   107.6%   95%[94, 123]     100%を含む
2025年     87.2%   95%[65, 114]     100%を含む
```

選別力があることと、控除率20%を超えて勝てることは別。
**「馬を並べる能力はあるが、それが控除率を超える証拠はない」**が現在地。

### 4. 開発用の数字を信じてはいけない

2021-2024では15構成中12構成が「95%下限100%超」だった。
2025年を外しただけで、ほぼ何でも良く見える。
**この12という数字自体が、探索の危うさを示している。**

## 次にやるなら

追い切り（chokyo_hc.csv・180MB）がモデルに1列も入っていない。
競馬ラボが持っていて我々に無い唯一の情報で、元データは手元にある。
**ただし別に事前登録を書いてから測ること。**ここに追記しない。
