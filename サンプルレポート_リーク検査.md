# バックテスト リーク検査レポート（サンプル）

検査日: 2026/08/27
対象: 競馬予測モデル（Python・295ファイル / 検証データ 207,518頭・14,972レース）

> これは実際の納品物と同じ形式のサンプルです。検査者自身のプロジェクトを検査しました。

---

## 総評

**確定した問題は 0件。**要対処が 1件、確認済みが 2件です。

静的検査ツールは【高】として28箇所を挙げましたが、**コードを読んだ結果すべて
意図的なもの**でした。ツールの出力をそのまま報告書にしていたら、
存在しない問題を28件報告することになっていました。

一方で、**ツールが【中】として挙げた1件は実害の実績があり**、対処が必要です。

---

## 実行したコマンド

```bash
python leak_audit.py --code .
python leak_audit.py --data history_marks.csv --id race_id --date 日付
python leak_audit.py --sample <払戻データ> --payoff 払戻
```

---

## 【要対処】1件

### 罠5：結合キーの型が固定されていない（94ファイル）

**所見**
`read_csv` を dtype 指定なしで読み、かつ同じファイル内で結合している箇所が
94ファイルあります。

**なぜ危険か**
列の中身が全部数字だと pandas が int64 として読み、`"03"` が `3` になります。
書き戻すと `"3"` になり、`"03"` 形式の相手と結合できません。

**このプロジェクトでの実害**
2件発生しています。

```
① 馬番のゼロ埋め漏れ → 的中54本が11本に見えていた
② 追記のたびに型が変わる → その日の最初の数レースだけ照合できなくなる
```

②が特に厄介です。別形式の値（`"04-06"` や `"-"`）が1行でも混じれば object のまま
なので**壊れません**。壊れるのは「その日の最初の数レース」だけで、極めて
気づきにくい形で欠落します。

**対処**

```python
# 危険
old = pd.read_csv(path, dtype={"race_id": str})

# 安全（結合キーを全部指定する）
old = pd.read_csv(path, dtype={"race_id": str, "組み合わせ": str, "馬番": str})
```

加えて、**結合できた件数を必ず印字**してください。0件ヒットは正常ではありません。

---

## 【確認済み・対処されている】2件

### 罠1：race_id は日付ではない

**検出結果**

```
[高] 'race_id' でソートしても時系列になりません（逆行 2件）
[高] 先頭8桁が日付として解釈できます（100%）
     実際の日付との一致は 0%。**日付ではありません**
```

**一致率0%** です。`race_id` は「年＋競馬場コード＋開催回＋日目＋レース番号」で、
先頭8桁は日付ではありません。しかし競馬場コードが月、開催回が日として
有効な範囲に収まるため、**パースが必ず成功します**（例外が出ない）。

**現状の対処**
実開催日テーブル（`_race_date_map()`）を引く方式に修正済みです。
カバレッジを確認したところ **1,974開催すべてで日付が引けており、欠損0件**でした。

**残る低リスク**
`features.py:856, 869` に、日付が引けない場合のフォールバックとして
`sort_values("race_id")` が残っています。現状は発火しませんが、
**発火すると静かに誤ったソートになります**。

対処案：フォールバック時に警告を出すか、例外にして止める。

```python
if _dt is None:
    raise RuntimeError("開催日テーブルが引けません。race_id順は時系列ではないため中止します")
```

### 罠3：探索したことに対する検定

**所見**
グリッド探索が8箇所あります（`itertools.product`）。

**現状の対処**
順列検定を実施済みです。

```
順列検定（132通りの券種×条件を込み）  p=0.0000（偽物の最大124.5%）
順列検定（判定5つ＋組み合わせを込み）  p=0.0000（偽物の最大125.0%）
```

**探索した全組み合わせを再現**して偽物の分布を作っており、
「最良条件だけをシャッフルする」という不十分な形になっていません。適切です。

---

## 【シロと判断】ツールが挙げたが問題なし

静的検査が【高】として挙げた28箇所を、すべてコードを読んで確認しました。

| 箇所 | ツールの指摘 | 実際 | 判定 |
|---|---|---|---|
| `features.py:856,869` | IDでソート | 主経路は `["_race_dt","race_id"]`。IDソートは日付が引けない場合のフォールバック。コメントに明記あり | **シロ**（上記の低リスクのみ） |
| `cover_opt.py:67` 他 | IDでソート | LightGBMのグループ化のため。同一レースの行をまとめる目的で、時系列とは無関係 | **シロ** |
| `audit_leak.py:83` | IDでソート | 検査コード自体。逆行を数えるために意図的にIDでソートしている | **シロ** |
| `market_free_model.py:294` 他 | train_test_split | 外側で `df[df["年"] <= 2024]` と時系列分割済み。この分割は訓練データを較正用に分けるもの | **シロ** |
| `baba_ablation.py:61` | train_test_split | 同上（較正用の分割） | **シロ** |

**補足（軽微）**
較正用の分割が `random_state=42` のランダム分割です。厳密には時点で分けるほうが
望ましいですが、テストデータへの漏れは無く、実害は小さいと判断します。

---

## 標本設計（罠6）

**検体** 7,135件 / 平均 117.4 / 標準偏差 758.0 / 中央値 0.0 / ゼロの割合 85.6%

```
[高] 分散が大きい構造です。少数の当たりが平均を作っています
     → 短期の平均値にはほとんど情報がありません

      点数     95%下限     95%上限
       100      42.2     385.2
       400      68.2     213.7
     1,600      87.5     158.8
     3,200      93.9     147.9
     6,400     100.9     136.9  ← ここで100を上回る
```

**「回収率100%超」を主張するには約6,400件必要**です。
現在の記録ペース（1開催日あたり約20点・年104開催日）では**約58か月**かかります。

**推奨**
回収率での証明は現実的でないため、**より収束の速い指標に切り替える**ことを勧めます。
このプロジェクトでは「3着以内に入る率」（検証値32.1%）を主指標にしており、
100点で±9.0pt、200点で±6.5pt に収まります。

**「儲かるか」の答えにはなりませんが、「壊れていないか」の検出には足ります。**
証明を諦めて監視に切り替える、という判断です。妥当だと考えます。

---

## この検査の範囲

**検査したのは既知6種類のみです。「通った＝安全」ではありません。**

| | 状態 |
|---|---|
| 罠1 時系列リーク | 検査した（対処済みを確認） |
| 罠2 意思決定時点に無い値 | **一部のみ**。列名からの推定は誤検出が多く無効化しています |
| 罠3 探索による選択 | 検査した（順列検定を確認） |
| 罠4 検証と本番の不一致 | **未検査**。機械では確かめられません（下記） |
| 罠5 集計・結合 | 検査した（要対処1件） |
| 罠6 標本不足 | 検査した |

### 機械では確かめられない2つ

**① 検証と本番で同じものを計算しているか**
別実装を書いていないか、モデル・前処理・使う確率が一致しているかは、
両方を実行して数字を突き合わせるしかありません。

このプロジェクトには `check_resid.py` があり、本番の関数に過去データを流して
基準値と一致するかを確認しています。**適切な作りです。**
ただし2026-08-17〜25の8日間、閾値変更時に基準値の更新を忘れ、
誤警告を出し続けていた期間がありました。**照合スクリプトは毎日自動で
走らせることを強く勧めます**（現在は日次点検に組み込み済み）。

**② 未知の罠**
7種類目は見つけられません。

---

## まとめ

```
要対処      1件  結合キーの型（94ファイル・実害2件の実績あり）
確認済み    2件  race_idの誤用 / 探索に対する検定
シロ       28件  ツールが挙げたが、読んだ結果すべて意図的
未検査      2件  検証と本番の一致 / 未知の罠
```

**最も重要な指摘は標本設計です。**回収率で優位を主張するには約58か月かかります。
この事実を知らずに短期の数字で判断すると、必ず誤ります。
