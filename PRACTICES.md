# 常に守ること（検証の作法・運用の習慣）

やり方を毎回思い出さなくて済むように、繰り返し必要になることだけを書いてある。
仕様は [SYSTEM.md](SYSTEM.md)、障害対処は [RUNBOOK.md](RUNBOOK.md)。

最終更新: 2026-08-11

---

## 1. 検証するときに必ず守ること

### ① 入力ファイルを間違えない

**回収率の検証は必ず `bet_cache_YYYY.csv` ＋ `jv_payouts.csv` で行う。**

`model_mf_result.csv` から自分で期待値を計算し直すと**再現しない**。
`bet_cache` には `EV_tan` / `mr` / `乖離` / `c_win_n` が計算済みで入っており、
過去の全ての検証結果はこれを前提にしている。

> 2026-08-10、これを取り違えて「2025年の単勝は78.8%」「馬連が馬単を上回る」と
> 2回誤報告した。正しい入力では111.5%で、結論も逆だった。

### ② `race_id` は必ず文字列で読む

```python
pd.read_csv(..., dtype={"race_id": str})
```

忘れると `int64` になり、払戻表（文字列キー）と**一致しないまま的中0本**になる。
エラーは出ない。

> 2026-08-11、`validate2026.py` でこれをやり、全条件が回収率0%になった。

### ③ `usecols` の順に列は返らない

pandas は**元ファイルの列順**で返す。`df.columns = [...]` で機械的に上書きすると
中身が入れ替わる。**必ず `rename` で名前対応させる。**

```python
# 危険
df = pd.read_csv(f, usecols=["race_id", "馬名", "単勝オッズ", "人気"])
df.columns = ["race_id", "馬名", "odds", "pop"]      # ← 入れ替わりうる

# 安全
df = pd.read_csv(f, usecols=[...]).rename(columns={"単勝オッズ": "odds", "人気": "pop"})
```

> 2026-08-11、これでオッズと人気が入れ替わり「オッズ中央値−68%」という
> あり得ない値が出た。異常値に気づいて追跡できたが、気づかなければ
> 「スリッページで壊滅」という誤結論になっていた。

### ④ 出た数字を疑う手順

異常に良い／悪い数字が出たら、**結論を書く前に**この順で確認する。

1. **物理的にあり得るか**（例: 1/オッズの和は1.2〜1.3。3.18なら列がおかしい）
2. **1レースだけ目視**で生データと突き合わせる
3. **突き合わせ件数**を数える（0件・極端に少ないなら結合が失敗している）
4. 単位・符号・年の範囲

### ⑤ 検証設定を本番と混同しない

比較用に学習を軽くした場合（LGB2シードなど）、**絶対値を本番の数字と並べない**。
差だけを見る。スクリプトの冒頭に必ず書いておく。

### ⑥ 多重比較を数える

条件を何通り試したかを**必ず記録する**。130通り試して1つが基準を通るのは
偶然でも起きる。採用の前に**独立した年で再現するか**を確認する。

---

## 2. 結論を出すときの基準

| 基準 | 値 |
|---|---|
| 全ての検証年で100%超 | 必須 |
| ブートストラップ95%下限 | **100%超が理想**。下回るなら「確証なし」と書く |
| 的中本数 | 50本以上。それ未満は「判定不能」 |
| 独立した年での再現 | 採用の必須条件 |

**「平均が高い」だけでは採用しない。** 平均が高い構成ほど下限が低い傾向がある。

**判定できないことは判定できないと書く。** これまでの経験上、的中20本以下の
比較はほぼ全て偶然の範囲だった。

---

## 3. 運用で常時やること

### 週1回（火曜の朝）

**週次更新メールを見るだけ。** 詳細は [RUNBOOK.md](RUNBOOK.md) 1章。

- `✅完了` → 何もしない
- `⚠失敗` → 本文にどのStepか出る
- **来ない → 最も危険**

### 月1回

`history_marks.csv` の行数が増えているか（開催日あたり約486行）。

### 変更を入れたとき

1. **本番の入力列（`FEATURE_COLS_MF`）を変えていないか**確認する
2. 変えたら**モデルの再学習が要る**＝蓄積の前提（同一モデル）が崩れる
3. `FREEZE_MODEL` がある間は、学習物を触る変更を入れない

### 新しい日次処理を足すとき

**必ず「今日のデータか」を最初に確認する。**
`today_*.csv` は開催日にしか更新されない。ガードが無いと前回の分を積み直す。

> 2026-08-10、`archive_daily` にこれが無く、非開催日に同じ36レースを
> 毎晩積み直す状態だった。半年で蓄積が分析不能になるところだった。
> `analyze_accuracy` でも同じ事故が起きている（8/2の結果が8/3・8/4にも記録）。

### 新しいスクレイピングを足すとき

- **実在するIDだけを取りに行く。** 総当たりは禁止
- 逐次のみ。並列にしない
- 取得済みは二度と取りに行かない
- フォールバックで総当たりに戻さない（取れないなら**何もしない**）

> 2026-08-09、週次のStep0が総当たりで約9,000リクエスト（98%が存在しない
> ページ）を投げていた。7/27のIPブロックの原因はこれと見られる。

---

## 4. 記録の残し方

| 何を | どこに |
|---|---|
| 検証の結果（効いた・効かなかった） | [SYSTEM.md](SYSTEM.md) 6章 |
| 障害と対策 | [SYSTEM.md](SYSTEM.md) 12章 ＋ [RUNBOOK.md](RUNBOOK.md) |
| 検証スクリプト | 消さずに残す（再実行できることに価値がある） |
| 生の結果CSV | `*_result.csv` として残す |

**効かなかったことも必ず残す。** 同じ実験を繰り返さないため。
現在10件が記録されている（速度指数・血統・Elo・調教・クッション値など）。

---

## 5. 忘れがちな前提

- **常駐プロセスは `Stop-Process` では止まらない。** `schtasks` を使う
- **タスクスケジューラ起動のプロセスは標準出力が消える。** ログはファイルに書く
- **「プロセスが存在する＝正常」ではない。** 心拍で確認する
- **モデル固定中**（`FREEZE_MODEL`）。学習物が古いのは正常
- **馬券は買っていない。** 記録だけを積んでいる
- 検証と実運用は**選ぶ馬が違う**（確定オッズ vs 7分前オッズ）。[SYSTEM.md](SYSTEM.md) 10.5章
