# 手離れする発信の作り方（Zenn自動公開 + X告知）

最終更新: 2026/08/25

## まずの目標：**月3,000円**（Claudeの月額を回収する）

これが最初の到達点です。桁で言うとこうなります。

| 手段 | 単価 | 必要数/月 |
|---|---|---|
| Zenn Books | 500円 | 6〜8冊（手数料ぶん多めに） |
| note 有料単発 | 300円 | 10〜11本 |

**達成できない最大の理由は、いまの流入がゼロに近いことです。**
note は全期間で42ビュー。ここを動かさない限り、何を出しても売れません。
だから最初にやるのは「売る」ではなく「読まれる」です。

---

## なぜ Zenn と X なのか

| | note | Zenn | X |
|---|---|---|---|
| 自動投稿 | **不可**（公式APIなし） | **可**（GitHub push で自動公開） | **可**（公式API・従量課金） |
| 規約 | ブラウザ自動化はグレー | 公式に想定された使い方 | 公式API |
| 費用 | 無料 | 無料 | 投稿1件 約1.5円 |
| 読者 | 一般・競馬ファン | 技術者 | 拡散の起点 |
| 売る力 | 強い | 弱い〜中 | 直接は売らない |

**Zenn を自動の主戦場、note を手動の収益源**にする二本立てにします。
X は集客の起点で、ここが無いと Zenn に書いても人が来ません。

---

## セットアップ（1回だけ。あなたの操作が要ります）

### 手順1：GitHub にリポジトリを作る

ローカルには既に `デスクトップ/keiba_zenn/` を作ってあります（記事2本入り）。

1. GitHub で新しいリポジトリを作る（名前の例：`keiba-zenn`）
   - **Public / Private どちらでも動きます**
   - README・.gitignore は付けない（既にローカルにあるため）
2. ローカルと繋ぐ

```bash
cd "C:/Users/別府飛河/OneDrive/デスクトップ/keiba_zenn"
git add .
git commit -m "Zenn記事の初期投入"
git branch -M main
git remote add origin https://github.com/fu-ga306/keiba-zenn.git
git push -u origin main
```

### 手順2：Zenn アカウントを作って GitHub と連携

1. https://zenn.dev/ で GitHub ログイン（アカウント作成）
2. ダッシュボード →「GitHubからのデプロイ」→ 手順1のリポジトリを選ぶ
3. 連携が完了すると、**push するたびに自動で反映**されます

連携後、`articles/` の2本が Zenn に現れます。ただし両方 `published: false` に
してあるので**まだ公開されません**。中身を確認してから `true` に変えてください。

### 手順3：X アカウントを作る

- 無料です。**ここが集客の起点**なので避けられません
- プロフィールに「競馬AIの検証記録を書いています」と Zenn へのリンクを置く

### 手順4：X API の設定（自動投稿する場合のみ）

自動投稿しないなら飛ばして構いません。手でツイートしても同じです。

1. https://developer.x.com/ で開発者登録
2. **最低 $5 のチャージ**（従量課金。投稿1件 $0.01）
3. アプリを作り、キーを4つ発行する
4. `keiba_ai/.env` に追記（**値だけ。`#` のコメントは付けない**）

```
X_API_KEY=xxxxx
X_API_SECRET=xxxxx
X_ACCESS_TOKEN=xxxxx
X_ACCESS_SECRET=xxxxx
```

1日3投稿なら月90件で約$0.90（135円）です。

---

## 動かし方

### いまの安全弁（すべて閉じています）

```bash
python x_post.py --status         # X投稿の設定を見る
python zenn_publish.py --status   # Zenn公開の設定を見る
```

| | 現在 | 実行するには |
|---|---|---|
| `x_post.DRY_RUN` | **True** | False にする |
| `X_POST_ARMED` ファイル | **無し** | 置く |
| X認証情報 | **未設定** | .env に4つ |
| `zenn_publish.DRY_RUN` | **True** | False にする |

**1つでも閉じていれば投稿されません。**

### 週次の記事を作る

```bash
python zenn_publish.py            # ドライラン（何も作らない）
python zenn_publish.py --push     # DRY_RUN=False なら実際に作って push
```

このスクリプトは `paper_report.py` の出力をそのまま記事に貼ります。
**数字を作文しません。**集計が異常終了したら記事を作らずに止まります。

### 予約投稿が肝です

生成される記事には `published_at` が**3日先**で入ります。

```
何もしなければ → その日時に自動公開（手離れ）
気に入らなければ → それまでにファイルを直すか消せば取り消せる（安全）
```

即時公開にすると変な記事を止められず、毎回確認すると手離れになりません。
その中間を取っています。日数は `DELAY_DAYS` で変えられます。

### X告知

`zenn_publish.py` は告知文を `x_queue.txt` に**入れるだけ**です。投稿はしません。

```bash
python x_post.py --queue          # キューを処理（安全弁を通る）
```

同じ本文は二度投稿しません。1日5件の上限もあります。

---

## 収益までの現実的な道のり

```
第1〜2週   セットアップ。Zenn記事2本を公開。X開始
           → 収益 0円。ここは種まき

第1〜2ヶ月  週次記録が自動で積み上がる
           X で技術系の話題として発信（競馬タグより機械学習タグ）
           → 月間ビューが3桁に乗るかを見る

3ヶ月〜     ビューが3桁に乗ったら Zenn Books（500円）を出す
           中身は既にある note/有料01 を書籍化するだけ
           → ここで初めて「売れるか」の実験が成立する
```

**月3,000円は、流入が3桁に乗ってからの話**です。それ以前に有料を出すと、
売れない理由が「需要が無い」のか「見られていない」のか分からなくなります。

### 数字の目安

Zenn で500円の技術書を出して5冊売れた、という個人開発者の報告があります。
**桁はそのくらい**です。月3,000円なら6〜8冊なので、決して低い目標ではありません。

ただし有利な点が2つあります。**中身が既にある**こと（8回失敗した台帳は他に無い）と、
**負けている限り材料が増える**ことです。

---

## 手離れできること・できないこと

| | |
|---|---|
| **できる** | 記事の生成、Zennへの公開、X告知、実績の更新、日次点検 |
| **できない** | note への投稿（APIが無い）、検証そのもの、記事の質の担保 |

**中身は自動生成できません。**正確には生成はできますが、元になる検証結果は
実際にレースを走らせないと出てきません。開催は月8日です。

「毎日3記事」のような量産は中身が空になるだけで、逆に読まれなくなります。
あなたの強みは他人が持っていない検証データなので、**量より継続**です。

---

## 注意

- `.env` は git管理外。X認証情報を**絶対にリポジトリに入れない**
- `keiba_zenn/` は `keiba_ai/` とは**別のリポジトリ**（入れ子にしない）
- Zenn の記事は公開されると検索に載る。**公開前に必ず一度は目視する**
  （予約投稿の3日間がその猶予です）
- 有料記事・有料本を出す時点で**特定商取引法に基づく表記**が必要
