# keiba_ai 開発ロードマップ・設計メモ

最終更新: 2026/06/15

---

## 🎯 最終目標（プロジェクトのゴール）

**推奨馬券を自動で購入するシステム**を最終形とする。
- 現状: AIが予想 → メール通知 → 手動購入
- 最終形: AIが予想 → 券種推奨 → 自動で馬券購入まで完結
- そのために必要な布石:
  - 券種推奨ロジックの確立（軸◎/相手○/穴▲ + 馬券種類拡大）← 進行中
  - 各馬券の回収率バックテストで「買う価値のある馬券」を厳選（払戻データB-3が前提）
  - 購入APIまたは自動操作の仕組み（IPAT連携等。将来の調査項目）
- ※ 自動購入は資金が動くため、精度・回収率の検証を十分に積んでから慎重に実装する

---

## A. マルチターゲット予想システム（新設計・実装待ち）

### ✅ 2026/06/15 実装状況（このセッションで完了）
- model.py: 3モデル(win/place2/place3)学習に拡張・train_all_targets()追加 → 完了・学習成功
  - 各モデル予測1位の的中率: win 33.0% / place2 52.1% / place3 64.3%（狙い通り複勝が高精度）
- keiba_predict.py / keiba_auto.py: 3モデル独立予想に対応・券種推奨ロジック実装 → 完了
- dashboard.py: 券種推奨列を追加 → 完了
- update_data.py: 再学習をtrain_all_targets呼び出しに変更 → 完了
- **案B（生確率統一）採用**: 勝率の正規化を廃止し、3モデルとも生確率に統一。
  これにより 勝率 ≤ 連対率 ≤ 複勝率 の包含関係が保証される（最も正確な実装）。
  - 副作用: 生確率は正規化版より小さく出る → 期待値が下がる
  - → 戦略の期待値しきい値を生確率ベースで取り直す必要（model.py期待値スイープで探索中）
  - keiba_auto.pyに EV_THRESHOLD_MAIN/SUB/TAIKO 定数を用意（スイープ結果で更新する）

### ⏳ 残（次回）
- model.py再実行の期待値スイープ結果を見て、最適しきい値を確定
- keiba_predict.py / keiba_auto.py の戦略しきい値（旧0.3/0.2/0.1）を最適値に更新
- 週末本番で券種推奨・3モデル予想の実戦動作を確認

### 背景・課題
現状は1着確率（勝ち確率）だけをモデルが出力し、連対率・複勝率はハーヴィルモデルで
数学的に変換しているだけ。そのため順位が絶対に入れ替わらず、「勝ちは平凡だが
複勝率は抜群」のような馬を検出できない。

### モデル層: 3つの独立モデル
目的変数だけ変え、特徴量・パイプラインは共通で流用する。

| モデル | 目的変数 | 出力 |
|--------|----------|------|
| win_model | 着順==1 | 勝率 |
| place2_model | 着順<=2 | 連対率 |
| place3_model | 着順<=3 | 複勝率 |

- 各モデルは現状と同じ3モデルアンサンブル（LightGBM + XGBoost + CatBoost）
- model.pkl に win / place2 / place3 の3セットを保存
- market_free_model も同様に拡張可能

### 券種推奨ロジック
| 印 | 条件 | 役割 |
|----|------|------|
| 軸馬◎ | win勝率が上位 かつ 勝ち期待値≥0 | 勝ちにいく信頼軸 |
| 軸馬(人気) | win勝率が上位 だが 勝ち期待値<0 | 実力はあるが妙味なし（除外せず表示） |
| 相手○ | place2連対率が高い | 軸の相手・馬連/ワイド |
| 穴馬▲ | place3複勝率にオッズ妙味あり or 勝ち期待値が高い一発 | 一発・複勝狙い |
| - | それ以外 | |

### 表示層（ダッシュボード）
- 勝率・連対率・複勝率を各独立モデルの実値で表示
- 券種推奨列に 軸◎ / 軸(人気) / 相手○ / 穴▲ / - を表示
- 「勝率は低いが複勝率は高い」馬が正しく見えるようにする

### 実装フェーズ（本番が動かないタイミングで着手）
1. model.py を3ターゲット学習に拡張（目的変数3種を作り学習・保存）
2. keiba_predict.py / keiba_auto.py を3モデル対応に（各馬に3確率を付与、券種推奨判定）
3. ダッシュボード表示を券種推奨に対応
4. （将来）払戻データ取得 → 複勝回収率バックテスト → 既存戦略と統合

### バックテストの展開方針（馬券種類拡大）
- 当面: 勝ち(win)モデルで従来通り単勝バックテスト（戦略A〜H）
- 将来: 馬券種類を増やすため、各モデルでバックテストを展開する
  - 連対(place2)モデル → 馬連・ワイドのバックテスト
  - 複勝(place3)モデル → 複勝のバックテスト
  - 各モデルの予想でその馬券を買った場合の回収率を検証
  - ※ 前提として馬連・ワイド・複勝の払戻データ取得が必要（B-3タスク）

### 決定事項
- 軸馬はB案（勝率上位かつ期待値≥0）。人気すぎる馬は除外せず「軸(人気)」と別表示。
- 評価は当面は的中率中心。複勝回収率の検証は払戻データ取得後。
- 既存の戦略A〜Hは当面併存。いずれ券種推奨に統合する。

---

## B. データ基盤タスク（netkeibaスクレイピング・本番非開催日に実施）

### B-1. 血統データ取得（horse_scraper.py）
- 父馬・母父馬を db.netkeiba.com から取得
- 進捗: 約4,151頭 / 19,722頭（カバー率約21%）
- IPブロック対策済み（待機5-9秒、driver使い回しのみ維持）
- 完走後 → sire_stats.py → features.py再実行 → モデル再学習
- 注意: レース日は完全停止。夜間・平日に数日かけて完走させる。

### B-2. レースクラス再取得（約3万レース）★優先度UP（2026/06/15重要度分析より）
- scraper.py のバグ修正済み（h1タグから正しくクラス取得）
- 既存31.8万行はクラス_numが壊れたまま → 全レース再スクレイピングが必要
- 完了すれば「距離×クラス_過去勝率」特徴量とクラス別バックテストが有効化
- コース形態（右/左等）・レース条件フラグ（牝馬限定戦等）も同じh1周辺なので統合取得可能
- 【重要】MF重要度分析で判明: クラス_num=0.00%（破損）、コース・馬場カテゴリ=1.0%
  → コース・馬場をモデルがほぼ無視 = ローカル場(福島47.5%)の弱点の主因
  → B-2でクラス修復＋コース形態取得すれば、0%・1%の領域を生き返らせられる
  → 血統と並ぶ最重要データ基盤タスク。コース形態(右左/直線長/坂)を必ず統合取得する

### B-3. 払戻データ取得（新規）
- 複勝・ワイド・馬連・馬単の実オッズ履歴を取得
- これらの券種の本格的なバックテストが可能になる
- マルチターゲットの複勝モデル評価に必須

---

## C. 特徴量・モデル改善タスク

### C-1. 追加済み・再学習待ちの特徴量
features.py に追加済みだが、features.py再実行＋モデル再学習がまだ。
- ペース想定特徴量4列（想定先行馬数/率、他馬想定先行馬数、差し馬×ハイペース想定）
- 体重増減の個体差2列（体重増減_過去標準偏差、体重増減_異常度）
- 【2026/06/15追加】距離適性・スタミナ系6列（長距離・距離延長の弱点対策）
  経験最長距離、経験範囲超過、延長×距離経験不足、長距離複勝率、前走余力、延長×前走余力
  - 背景: MFモデル弱点分析で「長距離(2001m〜)複勝率49%」「距離延長200m超で52.5%」
    「当たり外れを最も分ける要因=距離変化(差-4.69)」が判明 → 距離適性を強化
  - model.py / market_free_model.py の FEATURE_COLS に登録済み（MFは市場情報のみ除外）
  - ⚠️ keiba_auto.py の予測側(build_features)に6特徴量の計算がまだ未実装
    → 順番厳守: ①features再実行→再学習→効果検証 ②効果あればkeiba_auto予測側に実装
       ③両方そろってから本番反映（中途半端な状態で土曜本番を迎えないこと）
  - 効果検証: 再学習後に analyze_weakness.py 再実行で長距離複勝率が改善したか確認

### C-2. 未実装の特徴量候補
- 騎手の直近成績トレンド（直近1ヶ月の騎手勝率＝「今乗れている騎手」）
- コース形態変化（前走と今回で右/左・直線長さが変わる影響）※B-2で取得
- レース条件フラグ（牝馬限定戦等）※B-2で取得
- 距離変化×馬体重変化の交互作用
- 血統特徴量（父系/母父系×距離/競馬場/芝ダート適性）※B-1完了後

### C-3. モデル改善（再挑戦項目）
- 戦略Eの再設計（通常モデルとMFモデルの特徴量セットを差別化）
- LambdaRankの本格活用（スケール調整してアンサンブルに統合）
- Out-of-Foldスタッキング（リークなしで非均等アンサンブル）

---

## D. 運用・バグ修正タスク

### D-1. keiba_auto.py 停止エラー（低優先）
- `keiba_auto.py 停止エラー: [WinError 87] パラメーターが間違っています`
- 予想・メール・pushは完了しているので実害なし。プロセス停止ロジックを後で修正。

### D-2. keiba_auto.py スケジュール修正（完了・プッシュ済み）
- テスト実行モードになっていたのを本番スケジュール（発走7分前）に修正済み
- 来週土曜から各レース発走7分前にメールが届く

### D-3. 表示課題（低優先）
- keiba_predict.py のEV "+nan"・"nan倍" 表示をオッズ未確定時"-"に修正
- ダッシュボードの並び替えに「買い推奨順」を追加

---

## E. ダッシュボード機能追加（データ蓄積待ち・案A方式）

### E-1. 戦略該当のみ購入時の回収率表示
- keiba_auto.py修正により来週土曜から全レースが prediction_record_v2.csv に
  戦略フラグ付きで記録される
- データが揃ったら成績サマリーに「戦略該当のみ購入時の回収率」セクションを追加

---

## F. 精度分析の自動化（analyze_accuracy.py）

### F-1. 現状
- analyze_accuracy.py を作成済み（当日予想 vs 結果を6観点で分析）
  1. 印別の着順分布
  2. 予測順位 vs 実着順の相関
  3. 通常モデル vs 純粋AI(MF)の1位精度
  4. 戦略該当馬の実績
  5. 人気帯別のAI1位精度（人気追従でないか）
- まずは手動で1回実行して現モデルの弱点を把握する（次回夜に実施予定）

### F-2. 自動化（マルチターゲットモデル実装後に着手）
- 理由: 設計A(マルチターゲット)実装で予想構造が変わり、分析スクリプトも作り直しになる
  → 新モデル完成後に新スクリプトを自動化するのが無駄がない
- 設計案:
  - 毎日18:00にタスクスケジューラで analyze_accuracy.py を自動実行
    （全レース終了後・horse_scraper/keiba_autoと時間帯がずれる）
  - 結果を日付付きで accuracy_log.csv に追記蓄積
  - ダッシュボードに「精度推移」ページを追加して時系列で確認

### 実装順序（まとめ）
1. 手動で1回 analyze_accuracy.py 実行 → 現モデルの弱点把握（次回夜）
2. マルチターゲットモデル実装（設計A）
3. 新モデル対応の分析スクリプト作成 → 18時自動実行で蓄積

---

## 確定済みバックテスト結果（参考・75列3モデルアンサンブル時点）

| 戦略 | 回数 | 的中率 | 回収率 |
|------|------|--------|--------|
| 戦略A | 213 | 23.9% | 123.0% |
| 戦略A-2 | 135 | 23.7% | 142.3% |
| 戦略C | 51 | 27.5% | 203.7% |
| 戦略D(前走間隔2-4週) | 20 | 55.0% | 265.0% |
| 戦略F(東京中京) | 47 | 29.8% | 180.0% |
| 戦略FG(東京中京×短距離) | 10 | 40.0% | 265.0% |
| 戦略H(中山小倉) | 48 | 33.3% | 154.4% |

※ ⑤ペース想定・⑥体重異常度は未検証（features.py再実行＋再学習が必要）

---

## 環境メモ

- 場所: `c:\Users\別府飛河\OneDrive\デスクトップ\keiba_ai`
- GitHub: fu-ga306/keiba-ai
- Streamlit: keiba-ai-dashboard.streamlit.app
- Python実行: `& C:/Users/別府飛河/AppData/Local/Microsoft/WindowsApps/python3.11.exe <path>`
- ダッシュボード起動: `python3.11.exe -m streamlit run ...dashboard.py`（streamlit run必須）
- 当日自動運用: 06:55 タスクスケジューラ起動 → 07:00 朝の一括予想(keiba_predict today) → push、
  各レース発走7分前に keiba_auto.py が個別予想・オッズ取得・メール
- today_predictions.csv: 毎日上書き / prediction_record_v2.csv: 蓄積 / race_features.csv: 週次蓄積