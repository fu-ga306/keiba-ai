# 事前登録：レースごとに券種を選ぶ（2026-09-04）

**数字を見る前に、ここに書いたことだけを試す。あとから足さない。**

---

## やりたいこと

いまは全レースで同じ買い方（軸の単勝＋ダートならワイド）をしている。
**レースごとに、そのレースに合った券種を選べないか。**

材料は既にある。`resid_kinds_pred.csv` に

```
p1 / p2 / p3   モデルが出す 1着 / 2着以内 / 3着以内の確率
q / q2 / q3    市場が見ている同じもの
odds           単勝オッズ
```

これで各券種の期待値が組める。

---

## なぜ危ないか

**「レースごとに一番良い券種を選ぶ」は探索そのもの。**
選択肢を増やすほど、偶然良く見える組み合わせが必ず出る。

実測でも確かめている。払戻をシャッフルして中身をゼロにしたうえで
4つの帯から一番良いものを選ぶと、平均142%・最大216%が出た。

さらに、この設計は既に一度作られている（`resid_kinds.py`・2026-08-17）。
そのときの事前登録にはこう書かれていた。

```
④ 買うのは「期待値がしきい値を超えた券種のうち、期待値が最大のもの1点」
⑤ しきい値は単勝で決めた gap>=2.0 に相当する水準として、期待値 >= 2.0 に固定
⚠ 券種ごとにしきい値を変えると、それは探索になる。同じ数字を使う。
判定: 単勝のみの現行を上回らなければ広げない。
```

**この判定基準をそのまま引き継ぐ。**しきい値も券種ごとに変えない。

---

## 検証の枠組み

```
開発   2021 - 2024      ここで全部の試行を行う
評価   2025             最後に1回だけ見る
```

学習は毎年やり直す（既存の仕組みのまま）。

---

## 試すこと（この4つだけ。あとから足さない）

軸は現行のまま（gapが最大かつ 1.5以上の1頭）。**そのレースで何を買うかだけ変える。**

期待値は `モデルの確率 × オッズ`。オッズは単勝オッズからHarvilleで推定する
（馬連・ワイドの実オッズは事前に持っていないため。既存 `resid_kinds.py` と同じ）。

| | 中身 |
|---|---|
| A | 現行（単勝＋ダートならワイド）— 比較の基準 |
| B | 期待値が最大の券種を1点だけ買う（単勝 / 複勝 / ワイド / 馬連から） |
| C | 期待値がしきい値以上の券種を全部買う |
| D | 軸のgapで切り替える（gap高→単勝、gap低→複勝）。境目は中央値で固定 |

**しきい値は全券種で同じ値を使う。**券種ごとに変えたらそれは探索。

⚠ Dの「境目」は開発データの中央値で機械的に決める。**数字を見て動かさない。**

---

## 採用の規則（先に決める）

1. 2021-2024 の回収率だけでは採用しない。**95%区間が100%を上回ること。**
2. 区間が重なる候補が複数あるときは、**証明までの月数が最短のもの**を選ぶ。
3. 現行（A）が候補に残るなら、**現行を優先する。**同等なら変えない。
4. 選んだ構成を 2025 で1回だけ測る。そこで下がっても、選び直さない。

---

## 報告すること

- 試した全構成の数字（良いものだけ載せない）
- 2025年での最終評価
- 何も改善しなかった場合は、そう書く

---

## この検証で分からないこと

- **馬連・ワイドの実オッズを持っていない。**Harvilleの推定オッズを使う。
  推定が甘ければ期待値の計算もずれる。ここは構造的な弱点として残る。
- 2026年以降に通用するかは分からない。
