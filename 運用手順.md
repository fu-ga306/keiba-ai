# 運用手順

最終更新: 2026/08/28

**日常はこの1枚だけ見れば足ります。**

---

## いまの全体像

```
毎日  06:55  予想システム起動 → 36レース予想 → 7分前メール
      21:05  払戻を取得
      21:10  結果をアーカイブ
      21:20  自己点検（結果をメール）
      22:30  終了
20分ごと     見張り番。落ちていれば復旧・コード更新を検出して再起動

金06:00〜月09:00  ダッシュボードを公開（窓の外は自動停止）
金 21:20         点検メールに販売noteの下書きが届く
```

**あなたが手を動かすのは金曜だけです。**

---

## 毎週やること（金曜・5分）

### 1. メールを開く

```
件名: 【競馬AI 日次点検 ○高0/中N】MM/DD
```

冒頭を見ます。

```
重要度高 0件            ← 0以外なら本文を読む
7分前ジョブ: 送信N通    ← 予想したのに0通なら故障
撤退条件の判定日まで あとN日
```

### 2. 販売noteを貼る

本文の後半に **「今週の販売note（下書き・そのまま貼れます）」** があります。
これを note に貼って公開します。

**有料の設定はこうします。**

```
本文（先週の実績・較正表・何が見られるか）  → 無料
「ご購入後のご案内」から下（合言葉とURL）  → 有料エリア（300円）
```

note のエディタで「ここから先は有料」の線を入れる位置は、
下書きの中の **`## ご購入後のご案内`** の直前です。

> **なぜ本文を無料にするか**
> 売っているのは情報ではなく閲覧ページへの鍵です。
> 較正の実測表が購入の根拠そのものなので、隠すと判断材料が無くなります。
> 中身が見えない300円は買えません。

### 3. 合言葉を確認する（任意）

```bash
python sale_gate.py
```

下書きに埋め込まれているので、普段は見なくて構いません。

---

## 見るところ（週1回でよい）

```bash
python paper_report.py
```

**見るのは3つだけです。**

| 節 | 見るもの | 判断 |
|---|---|---|
| ① 買い率 | 検証値47.7%から大きく外れていないか | ズレていたら本番と検証で条件が違う |
| ⑤ 馬券内率 | 検証値32.1%の範囲に入っているか | 外れたらモデルの劣化を疑う |
| ③ 回収率 | **参考程度** | 58か月ぶん貯めないと判定できない |

**回収率で一喜一憂しないこと。**29点で20.7%でも、検証データから同じ29点を
取り出すと6%の確率で起きる範囲です。

---

## 何かおかしいと思ったら

```bash
python audit_system.py              システム全体の点検
python dashboard_service.py status  ダッシュボードが外から見えるか
python check_resid.py               買い目の実装が検証どおりか
python sale_gate.py                 今週の合言葉
```

### よくある症状と原因

| 症状 | 見るところ |
|---|---|
| 7分前メールが来ない | 買い判定が無い日は正常。予想したのに0通なら故障 |
| ダッシュボードが開かない | 窓の外（月09:00〜金06:00）なら正常。窓の中なら `status` |
| 「⚠算出不能」のメール | 残差モデルが動いていない。`check_resid.py` を実行 |
| ディスク不足の警告 | 2.0GB未満で予想が止まる。下の「容量が足りないとき」へ |

---

## 容量が足りないとき

この順で消せます。**上ほど安全です。**

```
1. bak_20260816/        136MB  リーク修正前のbet_cache。履歴的価値のみ
2. archive_*/                  古いアーカイブ
3. model_mf.pkl        2.6GB  model_mf_parts があれば不要かもしれない（要調査）
4. data/jv/            2.3GB  最後の手段。JV-VAN解約済みで二度と取得できない
```

`model_mf_parts.verified_0801/`（2.6GB）は2026/08/27に削除済みです。

---

## 撤退条件（2026/11/30に判定）

`撤退条件.md` に固定してあります。**判定日まで条件を動かしません。**

```
① Zenn 月間閲覧 300以上
② 有料が1本でも売れたか
③ 馬券内率が検証値の範囲内か
```

点検メールの冒頭に残り日数が出ます。

**どの結果でも予想システムは止めません。**自動なので手間がゼロだからです。
やめるのは「集客と販売」であって「検証」ではありません。

---

## 触らないほうがいいもの

| | 理由 |
|---|---|
| `BETTING_ENABLED = False` | 実測が足りない。変えると実弾が出る経路が開く |
| `resid_io.py` の閾値 | 事前登録した条件。動かすと検証が無効になる |
| `place_calib.pkl` | 販売表示の確率の根拠。作り直すなら検証もやり直す |
| `data/jv/` | 二度と取得できない生データ |

---

## 記録の場所

| | |
|---|---|
| 予想の記録 | `paper_resid.csv`（7分前の判断）/ `paper_resid_close.csv`（締切時） |
| 結果の履歴 | `history_marks.csv`（日次アーカイブが積む） |
| 販売の下書き | `note_weekly/YYYY-Wnn.md` |
| 未実施のこと | `未実施リスト.md` |
| 撤退条件 | `撤退条件.md` |

---

## 困ったときの原則

この半年で繰り返し起きたのは、**「動いているのに壊れている」**という形の故障でした。

```
メールが丸一日飛ばなかった      エラーは出ていない
検証ツールが8日間オオカミ少年   実装は正しいのに警告が出続けた
閲覧制限が完全に無効だった      テストは通っていた
```

だから、**動いているように見えても実際に確かめる**のが原則です。

- 数字が良く見えたら、まず測り方を疑う
- 「塞いだ」と思ったら、**実際のURLで試す**
- 件数が0なら、それは正常ではないかもしれない
