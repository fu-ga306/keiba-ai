# 自動購入の切り替え手順（2026-08-22 改訂）

## いまの状態：**5段のガードすべてが閉じている**

| # | ガード | 現在 | 意味 |
|---|---|---|---|
| ① | `keiba_predict.BETTING_ENABLED` | **False** | 買い目そのものが作られない |
| ② | `paper_resid.csv` の判定 | **全部「見送り」** | ①がFalseなので買い判定が付かない |
| ③ | `auto_vote.VOTE_MODE` | **dryrun** | 記録するだけ。実弾ゼロ |
| ④ | `AUTO_VOTE_ARMED` ファイル | **無い** | 物理スイッチが入っていない |
| ⑤ | `.env` の IPAT 認証 | **未設定** | ログインできない |

**1つでも閉じていれば1円も動きません。** いまは5つとも閉じています。

---

## 買い目の取得元（2026-08-22に対応済み）

```python
BET_SOURCE = "resid"      # 残差モデル（paper_resid.csv）← 既定
STAKE_PER_POINT = 100     # 1点100円。検証と同じ換算
```

以前は `today_bets.csv`（旧方式）しか読んでおらず、`BETTING_ENABLED` を True に
すると **検証していない旧方式が投票される**危険な状態だった。いまは残差モデルの
買い目を直接読む。`"legacy"` にすれば旧方式に戻せるが、通常は使わない。

**取得元を変えても実弾は出ない。** 実投票には別の4条件が要る（後述）。

---

## IPAT認証の設定

実投票にだけ必要。ドライランでは不要。`.env` に4つ書く。

```
IPAT_INET_ID=xxxxxxxx        # INET-ID
IPAT_SUBSCRIBER=xxxxxxxx     # 加入者番号
IPAT_PASSWORD=xxxx           # 暗証番号(4桁)
IPAT_PARS=xxxx               # P-ARS番号(4桁)
```

- `.env` は `.gitignore` 済み。GitHubには上がらない（確認済み）
- **1つでも欠ければ自動でドライランに降格する**ので、設定し忘れで実弾が出ることはない
- **入金は自動化していない。** 残高が無ければ投票は失敗する。先にIPAT側で入金すること

---

## 安全弁（検証した規模に合わせて縮小済み）

| 弁 | 変更前 | 現在 | 根拠 |
|---|---|---|---|
| `PER_RACE_MAX` | 5,000円 | **500円** | 残差モデルは1レース最大4点＝400円 |
| `DAILY_BUDGET_MAX` | 30,000円 | **3,000円** | 買うのは1日約18レース＝2,000円前後 |

上限が実際の規模より大きすぎると、**バグで点数が暴発したときに止められません**。
実際の使用量より少し上に置くのが安全です。

---

## 切り替えの前に満たすべき条件

前向き検証の目的は「バックテストが実測で再現するか」を確かめることです。
以下が揃うまでは切り替えないことを勧めます。

| 条件 | 根拠 | 現状 |
|---|---|---|
| **的中100本以上** | それ未満だと大穴1本で数字が反転する。5年検証でも60倍以上の帯は的中11本・95%区間[59.5, 288.4]で何も言えなかった | **0本** |
| **実測の回収率が100%超** | バックテスト120.6%が再現するか | 未測定 |
| **買い率がBTと整合** | ズレていれば本番と検証で条件が違う | 検証中 |

**過去8回、バックテストで良く見えたものがすべて崩れています。** 実測で確かめるまでは
paper trading を続けるのが安全です。

参考：年別は 137 / 116 / 111 / 163 / **79**% で、**5年に1回は負ける年**があります。

---

## 手順1〜4：切り替え（上から順に。1つずつ確認しながら）

### 1. 買い目の生成を開ける

`keiba_predict.py`：

```python
BETTING_ENABLED = True
```

この時点では **まだ実弾は出ません**（③④⑤が閉じているため）。
`today_bets.csv` が作られるので、**中身が残差モデルの買い目と一致するか**を確認します。

```bash
python check_resid.py      # 実装と検証の一致
python paper_report.py     # 記録との突き合わせ
```

### 2. ドライランで1開催ぶん確認

`vote_log.csv` に「投票予定」が記録されます。実弾は出ません。

確認すること：
- 点数と金額が想定どおりか（1レース1〜4点・1点100円）
- 日次上限に引っかかっていないか
- `paper_resid.csv` の買い目と一致するか

**最低1開催（土日）は必ずドライランで見ること。**

### 3. IPAT認証を設定

`.env` に追記します。

```
IPAT_INET_ID=xxxxx
IPAT_SUBSCRIBER=xxxxx
IPAT_PASSWORD=xxxx
IPAT_PARS=xxxx
```

⚠ `.env` は `.gitignore` に入っていることを確認してください。
**認証情報をGitHubに上げないこと。**

### 4. 実投票を有効化（最終段階）

```python
VOTE_MODE = "ipat"
```

さらに**物理スイッチ**を置きます。

```bash
touch AUTO_VOTE_ARMED
```

`auto_vote.py` 内の `_submit_ipat` の `SETUP_DONE = True` も必要です。

**この5つが全部揃って初めて実投票されます。** 1つでも欠ければ自動でドライランに降格します。

---

## 止め方（緊急時）

**一番速いのは物理スイッチを消すこと。**

```bash
rm AUTO_VOTE_ARMED
```

これだけで実投票は止まります（ドライランに降格）。コード編集は不要です。

完全に止めるなら：

```python
# keiba_predict.py
BETTING_ENABLED = False     # 買い目の生成そのものを止める
```

---

## 記録

すべての投票（予定・実行・スキップ）が `vote_log.csv` に残ります。
ドライランでも `mode=dryrun` / `status=投票予定` で残るので、後から突き合わせできます。

---

## 規約について

IPATの自動操作は**JRAの利用規約上グレー**です。自己資金・自己アカウントの
個人利用が前提で、判断は利用者の責任になります。

---

## まとめ：いま何をすべきか

```
今すぐ切り替える  → 勧めない。的中0本。数字を判断できる材料が無い
数か月後          → 的中100本・実測100%超を確認してから手順1へ
```

繋ぎ込み・IPAT手順・安全弁の縮小は**すべて済んでいます**。
残っているのは「実測で確かめる」ことだけです。
