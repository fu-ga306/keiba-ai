# 自動購入の切り替え手順（2026-08-22 改訂）

## いまの状態：**5段のガードすべてが閉じている**

| # | ガード | 現在 | 意味 |
|---|---|---|---|
| ① | `keiba_predict.BETTING_ENABLED` | **False** | 買い目そのものが作られない |
| ② | `today_bets.csv` | **存在しない** | 投票する対象が無い |
| ③ | `auto_vote.VOTE_MODE` | **dryrun** | 記録するだけ。実弾ゼロ |
| ④ | `AUTO_VOTE_ARMED` ファイル | **無い** | 物理スイッチが入っていない |
| ⑤ | `.env` の IPAT 認証 | **未設定** | ログインできない |

**1つでも閉じていれば1円も動きません。** いまは5つとも閉じています。

---

## ⚠ 最初に知っておくこと：**今の買い方は自動投票に繋がっていない**

```
残差モデル（★軸）→ paper_resid.csv  … 記録だけ。auto_vote は読まない
旧方式            → today_bets.csv   … auto_vote が読む。いま作られない
```

`auto_vote.py` は **`today_bets.csv` しか読みません**。いま実際に評価している
残差モデルの買い目は `paper_resid.csv` に記録しており、**別の場所**です。

つまり **①を True にしただけでは、残差モデルの買い目は投票されません。**
旧方式（購入停止した、100%を割る買い方）が復活するだけです。

**先に「残差モデルの買い目を `today_bets.csv` に書く」改修が要ります（後述の手順0）。**

---

## 切り替えの前に満たすべき条件

前向き検証の目的は「バックテストが実測で再現するか」を確かめることです。
以下が揃うまでは切り替えないことを勧めます。

| 条件 | 根拠 | 現状 |
|---|---|---|
| **的中100本以上** | それ未満だと大穴1本で数字が反転する。5年検証でも60倍以上の帯は的中11本・95%区間[59.5, 288.4]で何も言えなかった | **0本** |
| **実測の回収率が100%超** | バックテスト120.6%が再現するか | 未測定 |
| **買い率がBTと整合** | ズレていれば本番と検証で条件が違う | 検証中 |

**過去8回、バックテストで良く見えたものがすべて崩れています。** 実測で確かめるまでは
paper trading を続けるのが安全です。

参考：年別は 137 / 116 / 111 / 163 / **79**% で、**5年に1回は負ける年**があります。

---

## 手順0：残差モデルを投票経路に繋ぐ（未実施・要改修）

`keiba_predict.py` の `_build_bet_rows` が `today_bets.csv` を書きます。
ここに残差モデルの買い目（`resid_io.pick_bets` の結果）を流す必要があります。

```
現状: _race_bet_plan → 旧方式の判定 → today_bets.csv
必要: resid_io.pick_bets → today_bets.csv
```

**この改修をせずに①を開けると、検証していない旧方式が動きます。** 最も危険な誤りです。

---

## 手順1〜5：切り替え（上から順に。1つずつ確認しながら）

### 1. 少額から始める

`auto_vote.py` の冒頭を編集します。

```python
DAILY_BUDGET_MAX = 3000    # まず1日3,000円（既定30,000から下げる）
PER_RACE_MAX     = 500     # 1レース500円（既定5,000から下げる）
```

**いきなり既定値で始めないこと。** 挙動が想定どおりか確かめるのが先です。

### 2. 買い目の生成を開ける

`keiba_predict.py`：

```python
BETTING_ENABLED = True
```

この時点では **まだ実弾は出ません**（③④⑤が閉じているため）。
`today_bets.csv` が作られるので、**中身が残差モデルの買い目と一致するか**を確認します。

```bash
python check_resid.py      # 実装と検証の一致
python paper_report.py     # 記録との突き合わせ
```

### 3. ドライランで1開催ぶん確認

`vote_log.csv` に「投票予定」が記録されます。実弾は出ません。

確認すること：
- 点数と金額が想定どおりか（1レース1〜4点・1点500円）
- 日次上限に引っかかっていないか
- `paper_resid.csv` の買い目と一致するか

**最低1開催（土日）は必ずドライランで見ること。**

### 4. IPAT認証を設定

`.env` に追記します。

```
IPAT_INET_ID=xxxxx
IPAT_SUBSCRIBER=xxxxx
IPAT_PASSWORD=xxxx
IPAT_PARS=xxxx
```

⚠ `.env` は `.gitignore` に入っていることを確認してください。
**認証情報をGitHubに上げないこと。**

### 5. 実投票を有効化（最終段階）

```python
VOTE_MODE = "ipat"
```

さらに**物理スイッチ**を置きます。

```bash
touch AUTO_VOTE_ARMED
```

`auto_vote.py` 内の `_submit_ipat` の `SETUP_DONE = True` も必要です。

**この5つが全部揃って初めて実投票されます。** 1つでも欠ければ自動でドライランに降格します。

---

## 止め方（緊急時）

**一番速いのは物理スイッチを消すこと。**

```bash
rm AUTO_VOTE_ARMED
```

これだけで実投票は止まります（ドライランに降格）。コード編集は不要です。

完全に止めるなら：

```python
# keiba_predict.py
BETTING_ENABLED = False     # 買い目の生成そのものを止める
```

---

## 安全弁（自動で効くもの）

| 弁 | 既定 | 動作 |
|---|---|---|
| `DAILY_BUDGET_MAX` | 30,000円 | 1日の総額。超えたら以降スキップ |
| `PER_RACE_MAX` | 5,000円 | 1レースの上限 |
| `DAILY_LOSS_STOP` | — | 1日の損失が閾値を超えたら停止 |

すべての投票（予定・実行・スキップ）が `vote_log.csv` に残ります。

---

## 規約について

IPATの自動操作は**JRAの利用規約上グレー**です。自己資金・自己アカウントの
個人利用が前提で、判断は利用者の責任になります。

---

## まとめ：いま何をすべきか

```
今すぐ切り替える  → 勧めない。的中0本で、繋ぎ込みも未実施
数か月後          → 的中100本・実測100%超を確認してから手順0へ
```

**手順0（残差モデルを投票経路に繋ぐ）は、切り替えを決めてから行えば十分です。**
先に作っておくと、うっかり①を開けたときに実弾が出る経路ができてしまいます。
